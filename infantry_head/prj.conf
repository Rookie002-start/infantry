# Common settings for all projects
CONFIG_CPP=y
CONFIG_REQUIRES_FULL_LIBCPP=y
CONFIG_STD_CPP17=y
CONFIG_STATIC_INIT_GNU=y
CONFIG_THREAD_NAME=y
CONFIG_SYS_CLOCK_TICKS_PER_SEC=2000
CONFIG_TICKLESS_KERNEL=n
CONFIG_PICOLIBC_IO_FLOAT=y
CONFIG_FPU=y
CONFIG_FPU_SHARING=y
CONFIG_ZBUS_PRIORITY_BOOST=n
CONFIG_DCACHE=n

# 项目级基础设施
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_LOG_MODE_IMMEDIATE=y

# ---- SEGGER RTT（实时日志 / 变量打印，走调试探针，不占串口）----------------
# 查看方法见 docs/segger_rtt.md；不需要时把 CONFIG_DEBUG_RTT 关掉即可，
# 或整体退出模块：根 CMakeLists.txt 的 ZEPHYR_EXTRA_MODULES / Kconfig 里各一处。
CONFIG_DEBUG_RTT=n
# 上行缓冲（目标 -> 主机）：越大越不容易丢日志，代价是 RAM
CONFIG_SEGGER_RTT_BUFFER_SIZE_UP=4096
# 下行缓冲（主机 -> 目标）：给 RTT 输入通道留一点，暂时用不到可以留默认 16
CONFIG_SEGGER_RTT_BUFFER_SIZE_DOWN=32
# SEGGER_RTT_printf() 的内部攒包缓冲（此值越大越省事，越大越占栈）
CONFIG_SEGGER_RTT_PRINTF_BUFFER_SIZE=128
# 通道 0 模式：主机没连 / 缓冲满时直接丢，绝不阻塞控制线程
CONFIG_SEGGER_RTT_MODE_NO_BLOCK_SKIP=y
# 日志后端缓冲满时覆盖最旧数据（另一个选择是 BLOCK，会阻塞线程，别用）
CONFIG_LOG_BACKEND_RTT_MODE_OVERWRITE=y

# 全局 -O2 优化，替代默认 -Os
CONFIG_SPEED_OPTIMIZATIONS=y

CONFIG_PRJ_MAIN=y

#本项目线程开关
# CONFIG_TRD_IMU=y
# CONFIG_TRD_IMU_SRC_HIPNUC=y
# CONFIG_TRD_REMOTE=y
# CONFIG_TRD_MCU_INTER=y
# CONFIG_TRD_TEST=y
# CONFIG_TRD_CAN=y
CONFIG_TRD_PC=y

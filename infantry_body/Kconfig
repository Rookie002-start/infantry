# temp/Kconfig - 用户区业务 Kconfig（位置：/home/rookie/zephyrproject/projects/temp）
# 业务门禁符号（PRJ_MAIN / TRD_XXX）保持原名，架构层 DUST_ 符号由
# framework 模块机制自动加载（module.yml kconfig: zephyr/Kconfig）。

config DEBUG_LOG
    bool "Enable Zephyr LOG output"
    default n
    select LOG
    select LOG_BACKEND_UART

config DEBUG_RTT
    bool "SEGGER RTT debug channel (log + variable print over probe)"
    default n
    select LOG
    select HAS_SEGGER_RTT
    select USE_SEGGER_RTT
    select LOG_BACKEND_RTT
    select SEGGER_RTT_CUSTOM_LOCKING
    help
      打开 SEGGER RTT：日志（LOG_INF/WRN/ERR）实时输出到调试探针的 RTT 通道，
      业务代码也可以用 SEGGER_RTT_printf() 直接打变量，不占 UART。

      RTT 走调试接口（SWD/JTAG）读写目标 RAM，所以需要 J-Link / OpenOCD
      之类的调试器在线；查看方法见 docs/segger_rtt.md。

      HAS_SEGGER_RTT 通常由 SoC 自己 select（STM32 会），这里显式 select
      让非 STM32 板卡也能打开。

config PRJ_MAIN
    bool "Main business project"
    default n
    select DEBUG_LOG

if PRJ_MAIN

# =================               TRD_CONFIG                ====================

config TRD_GPIO
    bool "GPIO thread task"
    default n
    help
      Enable the GPIO idle thread.

config TRD_CAN
    bool "CAN thread task"
    default n
    select DUST_COM_CAN
    select DUST_TPC_TO_CAN_TX
    help
      Enable the CAN idle thread.

config TRD_CHASSIS
    bool "Chassis control thread"
    default n
    select TRD_CAN
    select DUST_COM_CAN
    select DUST_CTL_PID
    select DUST_MOD_DEV_MOTOR_DJI
    select DUST_TPC_TO_CAN_TX
    select DUST_TPC_FROM_HEAD
    select DUST_TPC_GIMBAL_TO
    select INTER_CMD
    help
      底盘控制线程（thread/chassis/trd_chassis.cpp）：1ms 周期

config TRD_GIMBAL
    bool "Gimbal control thread"
    default n
    select DUST_COM_CAN
    select DUST_CTL_PID
    select DUST_MOD_DEV_MOTOR_DM
    select DUST_TPC_TO_CAN_TX
    select DUST_TPC_FROM_HEAD
    select DUST_TPC_GIMBAL_TO
    select INTER_CMD
    help
      云台控制线程（thread/gimbal/trd_gimbal.cpp）：1ms 周期，
      yaw/pitch 各一台 DM 电机，双环 PID（角度环 → 角速度环 → 力矩）。

      本线程是 user-can3 总线的所有者（Init + 收帧分发入口），
      控制帧经 topic::to_can_tx 的 user_can3_msgq 外发；
      目标角来自 pub_from_head（上板指令），实测角经 inter_cmd Dir::Down
      回发上板，同时把"云台相对底盘 yaw"发布到 pub_gimbal_to 供底盘使用。
      设备树依赖：别名 user-can3，见 boards/*/*.overlay。

choice TRD_GIMBAL_FB_SOURCE
    prompt "Gimbal angle feedback"
    default TRD_GIMBAL_FB_ENCODER
    depends on TRD_GIMBAL
    help
      位置环的角度反馈来源，两种模式互斥（Kconfig choice）。
      两种模式都要求 IMU/编码器与电机正方向一致，方向不符时改
      trd_gimbal.cpp 里的 k*MotorSign / k*ImuSign。

config TRD_GIMBAL_FB_ENCODER
    bool "Motor encoder (relative to chassis)"
    help
      用 DM 电机编码器角度做反馈：控制的是"云台相对底盘"的角，
      底盘自转时云台跟着转。不依赖 IMU，作为默认模式。

config TRD_GIMBAL_FB_IMU
    bool "IMU absolute angle (IMU must follow the gimbal)"
    select TRD_IMU
    select DUST_TPC_IMU_TO
    help
      用 imu_to 的 yaw_total/pitch 做反馈：控制世界系绝对角，
      底盘自转时云台可稳住指向。要求 IMU 随云台运动（装在云台上），
      且 IMU 掉线即不再驱动电机（保持 0 力矩）。
      注意 IMU 源要与板级一致（dm_mc02 上是 BMI088，不是 HiPNUC）。
endchoice

config TRD_MCU_INTER
    bool "Inter-board FDCAN TX thread (lower -> upper)"
    default n
    select DUST_COM_CAN
    select TRD_INTER_BUS
    select DUST_TPC_TO_MCU_TX
    select DUST_TPC_GIMBAL_TO
    select INTER_CMD
    help
      下板 → 上板发送线程：聚合 Dir::Down 状态分片，定长发出到 0x200。
      总线初始化由 TRD_INTER_BUS 负责（不再绑在发送线程上）。
      Note: 依赖设备树别名 inter-can，仅在有该总线的板卡上开启。

config TRD_INTER_RX
    bool "Inter-board RX thread (from upper board)"
    default n
    select DUST_COM_CAN
    select TRD_INTER_BUS
    select DUST_TPC_FROM_HEAD
    select INTER_CMD
    help
      Enable the upper-board receive thread: state frame (zbus publish + timeout
      strategy). Requires DT alias inter-can.

config TRD_INTER_BUS
    bool "Inter-board CAN bus owner (init only, no thread)"
    default n
    select DUST_COM_CAN
    help
      上下板通信总线所有者：只做 Can::Init(..., CAN_MODE_FD) + SetRxCallback，不建线程。
      由 TRD_MCU_INTER / TRD_INTER_RX 自动 select；也可单独打开用于
      "只调接收" / "只调发送"的调试场景。依赖设备树别名 inter-can。

config TRD_TEST
    bool "TEST thread task"
    default n
    help
      Enable the TEST thread.

config TRD_REMOTE
    bool "REMOTE thread task"
    default n
    select DUST_MOD_DEV_REMOTE
    select DUST_MOD_DEV_REMOTE_VT13
    help
      Enable the REMOTE thread.

config TRD_IMU
    bool "IMU thread task"
    default n
    select DUST_MOD_DEV_IMU
    help
      Enable the IMU thread.

choice TRD_IMU_SOURCE
    prompt "IMU source"
    default TRD_IMU_SRC_HIPNUC
    depends on TRD_IMU
    help
      选择底层 IMU 数据源。三种源互斥：链接段 .imu 只允许注册一个设备。

config TRD_IMU_SRC_BMI088
    bool "BMI088 (SPI, with heater)"
    select DUST_MOD_DEV_IMU_BMI088

config TRD_IMU_SRC_ICM42688P
    bool "ICM42688P (SPI, with heater)"
    select DUST_MOD_DEV_IMU_ICM42688P

config TRD_IMU_SRC_HIPNUC
    bool "HiPNUC (UART, fused output)"
    select DUST_MOD_DEV_IMU_HIPNUC

endchoice

endif # PRJ_MAIN

source "Kconfig.zephyr"
rsource "my_topic/Kconfig"
rsource "inter_cmd/Kconfig"
